div
    a(href="/#/") Click here to go back to the main page
  .row
    .tabbable
      h3 Forms
      ul.nav.nav-tabs
        li.active#get-nav
          a(ng-click="select('get')") GET Form
        li#post-nav
          a(ng-click="select('post')") POST Form
        li#put-nav
          a(ng-click="select('put')") PUT Form
        li#delete-nav
          a(ng-click="select('delete')") DELETE Form

      .tab-content
        .tab-pane.active#get-pane
          p 
            strong Explanation:
            |  GET gets a message from the server given an id.
          form.sample-form#get-form(ng-submit="submitGet()")
            label ID
            input.id(ng-model='id', type='text', name='id', value="1")
            label Message
            textarea.message(ng-model='message', disabled, placeholder='Message goes here...', name='message')
            div
              button.btn#get-button Retrieve Message

          
        .tab-pane#post-pane
          p 
            strong Explanation:
            |  POST creates a new message on the server. The id of the new message will be displayed afterwards.
          form.sample-form#post-form(ng-submit="submitPost()")
            label ID
            input.id(ng-model='id', disabled, type='text', name='id')
            label Message
            textarea.message(ng-model='message', placeholder='Put your message here.', name='message')
            div
              button.btn#post-button Create Message

          
        .tab-pane#put-pane
          p 
            strong Explanation:
            |  PUT updates a message on the server given the id and the new message.
          form.sample-form#put-form(ng-submit="submitPut()")
            label ID
            input.id(ng-model='id', type='text', name='id')
            label Message
            textarea.message(ng-model='message', placeholder='Put your message here.', name='message')
            div
              button.btn#put-button Update Message

          
        .tab-pane#delete-pane
          p 
            strong Explanation:
            |  DELETE deletes a message on the server given the id.
          form.sample-form#delete-form(ng-submit="submitDelete()")
            label ID
            input.id(ng-model='id', type='text', name='id')
            div
              button.btn#delete-button Delete Message
